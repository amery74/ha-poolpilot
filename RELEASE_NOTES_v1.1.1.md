# Pool Pilot v1.1.1

Maintenance release focused on installation and configuration.

## Changes

- The pump command is now optional.
- Added a separate optional pump-state entity.
- `binary_sensor`, `switch`, and `input_boolean` can be used to read pump state.
- Only `switch` and `input_boolean` are used for pump control.
- Improved setup labels and documentation.
